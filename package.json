{
  "name": "budaxov",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm --prefix backend install",
    "start": "npm --prefix backend run db:generate && npm --prefix backend run db:push && node backend/src/index.js"
  }
}
