{
  "name": "budaxov",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix backend run db:generate && npm --prefix frontend run build",
    "start": "node scripts/start.js"
  }
}
